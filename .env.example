# Charteris Environment Variables
# Copy to .env and fill in values. Do NOT commit .env to git.

# API token — required on all API/WebSocket/MCP requests (?token=<value>)
CHARTERIS_API_TOKEN=changeme-api-token-secret
